---
phase: 06-product-surface-hardening-for-cli-doctor-examples-and-docs
plan: 02
type: execute
wave: 1
depends_on:
  - 06-01
files_modified:
  - src/main.rs
  - README.md
  - docs/ROADMAP.md
autonomous: true
requirements:
  - UX-V2-01

must_haves:
  truths:
    - "README no longer claims Adam is 1.9x slower — reflects Phase 1 result (+25% faster)"
    - "README Roadmap snapshot no longer lists Adam fusion as next step"
    - "USAGE/--help mentions that compile requires llvm-codegen feature"
    - "docs/ROADMAP.md reflects Phases 1-5 complete, Phase 6 in progress"
    - "export-py appears in the README CLI table"
  artifacts:
    - path: "src/main.rs"
      provides: "Updated USAGE string with llvm-codegen note"
    - path: "README.md"
      provides: "Accurate Limitations section and Roadmap snapshot"
    - path: "docs/ROADMAP.md"
      provides: "Synced public roadmap reflecting Phases 1-5 completion"
  key_links:
    - from: "README.md Limitations"
      to: "docs/BENCHMARKS.md Adam row"
      via: "Same Adam benchmark claim must match"
      pattern: "Adam.*faster|Adam.*slower"
---

<objective>
Fix stale and incorrect text in CLI help/USAGE, README.md, and docs/ROADMAP.md so that what users read matches what the code actually does.

Purpose: The README still says "Adam is 1.9× slower" (false since Phase 1) and the roadmap snapshot lists Adam fusion as "next" (5 phases stale). USAGE doesn't mention the llvm-codegen feature requirement for `compile`. These are factual errors that mislead users about what Volta can do today.

Output: Three files updated. No new code logic. No new dependencies. Purely text/prose corrections.
</objective>

<execution_context>
@C:/Users/User/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/User/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-product-surface-hardening-for-cli-doctor-examples-and-docs/06-RESEARCH.md

<interfaces>
<!-- Current stale claims found by research (from 06-RESEARCH.md): -->

README.md line 29: "Adam optimizer is currently 1.9× slower than PyTorch"
  → WRONG. Phase 1 fixed this. Current: Adam +25% faster (4.259 ms vs PyTorch 5.343 ms)

README.md "Roadmap snapshot" section:
  - "Next: Adam fusion (close the 1.9× gap), autotune tile sizes, cross-platform build"
  → WRONG. Adam was fixed Phase 1. Phases 1-5 are complete.

README.md CLI table: missing export-py command

USAGE string in src/main.rs (lines 19-30):
  "volta compile <file.vt> [-o <output>]"
  → Missing: requires --features llvm-codegen (fails at runtime with opaque message)

docs/ROADMAP.md: references old Phase 1-5 internal names, states "Adam 1.9× slower" in historical note

Current accurate Adam benchmark (from docs/BENCHMARKS.md and STATE.md):
  Adam: 4.259 ms vs PyTorch 5.343 ms = 0.797× (+25% faster)

Current phase status (from .planning/ROADMAP.md):
  Phase 1-5: Complete
  Phase 6: In progress (product surface hardening)
  Phase 7: Planned (packaging)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix README stale claims — Limitations and Roadmap snapshot</name>
  <files>README.md</files>
  <action>
Read README.md in full. Make the following targeted edits:

1. In "Current strengths / current limitations" section, find the line:
   "Adam optimizer is currently 1.9× slower than PyTorch (separate dW GEMM, no fusion)"
   Replace with:
   "Adam optimizer is +25% faster than PyTorch on B≤64 MLP (4.3 ms vs 5.3 ms; SGD is 35-67% faster)"
   This matches STATE.md metrics and docs/BENCHMARKS.md.

2. In "Benchmark highlights" section, the honest note at line 71 currently says:
   "Adam optimizer is slower in Volta."
   Replace with:
   "Adam optimizer is +25% faster than PyTorch at B≤64."

3. In "Roadmap snapshot" section, replace the content:
   Current:
   ```
   - **Done**: interpreter, IR, codegen, SGD training DLL, AVX2 kernels, MKL hybrid, benchmark harness
   - **Next**: Adam fusion (close the 1.9× gap), autotune tile sizes, cross-platform build
   - **Later**: GPU (CUDA) perf measurement, broader model coverage beyond MLP
   ```
   Replace with:
   ```
   - **Done (Phases 1–5)**: interpreter, IR, codegen, SGD + Adam training DLL, AVX2 kernels, MKL hybrid, benchmark harness, end-to-end PyTorch parity for MLP/ConvNet/tiny-transformer
   - **In progress (Phase 6)**: CLI/help/doctor/examples/docs aligned with real behaviour
   - **Planned (Phase 7)**: release artifacts, clean install story, smoke-tested shipped binary
   - **Later**: autotune tile sizes, cross-platform build, broader model coverage beyond MLP
   ```

4. In the CLI table (lines 98-107), add `export-py` row:
   After `volta doctor` line, add:
   `volta export-py <file.vt>        # Export model as Python/PyTorch code`

Write back the updated README.md.
  </action>
  <verify>
    <automated>cargo test --quiet 2>&1</automated>
  </verify>
  <done>
    README.md no longer contains "1.9× slower" or "Adam fusion" as a next step. Roadmap snapshot reflects Phases 1-5 complete. export-py appears in CLI table. `cargo test --quiet` still passes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix USAGE string in src/main.rs and update docs/ROADMAP.md</name>
  <files>src/main.rs, docs/ROADMAP.md</files>
  <action>
**src/main.rs — USAGE string update:**

Read src/main.rs. Find the USAGE const (lines 19-30). Make the following changes:

1. Change the `compile` line from:
   `  volta compile <file.vt> [-o <output>]`
   to:
   `  volta compile <file.vt> [-o <output>]       (requires --features llvm-codegen)`

2. Change the `extract` line from:
   `  volta extract <model_name>`
   to:
   `  volta extract <file.gguf|file.safetensors>  (GGUF/SafeTensors → .vt)`
   This matches the actual usage — it takes a file path, not a logical model name.

No other logic changes to src/main.rs. Only the USAGE const string.

**docs/ROADMAP.md — sync to current state:**

Read docs/ROADMAP.md. Rewrite it to reflect current reality:

```markdown
# Volta — Public Roadmap

Last updated: 2026-03-08

## Completed

- **Phase 1 — Adam Performance**: Adam optimizer reaches PyTorch parity (+25% faster at B≤64). SGD regression gate maintained.
- **Phase 2 — Infrastructure**: Portable gemm_shim, automatic RUSTFLAGS propagation, explicit backend capabilities matrix.
- **Phase 3 — Reliability and Correctness**: MHA full backward pass, panic-free autograd, Tensor::PartialEq.
- **Phase 4 — CPU Training Path Hardening**: Long-loop stability, fail-fast non-finite guards, compile-reuse regression gate.
- **Phase 5 — End-to-End PyTorch Parity**: ConvNet and tiny-transformer parity against PyTorch. AOT training codegen confirmed MLP-only (explicit truth-pass).

## In Progress

- **Phase 6 — Product Surface Hardening**: CLI help/output, `doctor` capability matrix and actionable diagnostics, examples and docs aligned with real behaviour.

## Planned

- **Phase 7 — Packaging and Install Story**: Release artifacts, clean install verification, smoke-tested shipped binary.

## Later / Unmapped

- Autotune tile sizes (PERF-V2-01)
- Cross-platform build (PLAT-V2-01, PLAT-V2-02)
- Broader AOT model coverage beyond MLP (MODEL-V2-01)
- CUDA path benchmarking and verification (CUDA-V2-01 through CUDA-V2-03)
```

Write back both files.
  </action>
  <verify>
    <automated>cargo test --quiet 2>&1</automated>
  </verify>
  <done>
    src/main.rs USAGE shows llvm-codegen note on `compile` and correct description for `extract`. docs/ROADMAP.md reflects Phases 1-5 complete and Phase 6 in progress. `cargo test --quiet` passes (USAGE string change may affect a parse test — fix any affected test).
  </done>
</task>

</tasks>

<verification>
`cargo test --quiet` passes after both tasks.
README.md does not contain "1.9× slower".
USAGE in src/main.rs contains "llvm-codegen".
docs/ROADMAP.md no longer references the old internal phase names with stale status.
</verification>

<success_criteria>
- README.md Adam claim corrected to +25% faster
- README.md Roadmap snapshot reflects Phases 1-5 done, Phase 6 in progress
- README.md CLI table includes export-py
- src/main.rs USAGE mentions llvm-codegen feature for `compile`
- src/main.rs USAGE `extract` description mentions file path, not logical model name
- docs/ROADMAP.md synced to current phase completion state
- `cargo test --quiet` exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/06-product-surface-hardening-for-cli-doctor-examples-and-docs/06-02-SUMMARY.md`
</output>
