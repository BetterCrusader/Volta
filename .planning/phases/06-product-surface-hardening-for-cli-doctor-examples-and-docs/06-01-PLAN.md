---
phase: 06-product-surface-hardening-for-cli-doctor-examples-and-docs
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/cli_smoke.rs
autonomous: true
requirements:
  - UX-V2-02
  - UX-V2-03

must_haves:
  truths:
    - "cargo test --test cli_smoke compiles without error"
    - "All smoke test stubs are present as named functions matching the verification map in 06-VALIDATION.md"
    - "Tests that require a built binary use CARGO_BIN_EXE_volta, not cargo run"
  artifacts:
    - path: "tests/cli_smoke.rs"
      provides: "CLI integration smoke test scaffold"
      exports:
        - smoke_version
        - smoke_doctor
        - smoke_run_xor
        - smoke_check_bench_real
        - smoke_compile_train_rejects_non_mlp
        - doctor_json_fields
  key_links:
    - from: "tests/cli_smoke.rs"
      to: "target/release/volta binary"
      via: "env!(\"CARGO_BIN_EXE_volta\")"
      pattern: "CARGO_BIN_EXE_volta"
---

<objective>
Create the `tests/cli_smoke.rs` integration test file that is the Wave 0 prerequisite for all Phase 6 automated verification.

Purpose: Every subsequent plan in Phase 6 (doctor rewrite, CLI fixes, examples smoke) needs automated test coverage in `tests/cli_smoke.rs`. Without this file existing, subsequent `<automated>` verify commands fail with "no integration test target". Creating it first as a Wave 0 plan unblocks all parallel Wave 1 plans.

Output: `tests/cli_smoke.rs` with all required test function stubs, compiling cleanly, using `CARGO_BIN_EXE_volta` pattern. Stubs that test functionality not yet implemented (e.g., doctor MKL section) may initially assert only that the binary runs without panic, and are tightened in later plans.
</objective>

<execution_context>
@C:/Users/User/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/User/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-product-surface-hardening-for-cli-doctor-examples-and-docs/06-VALIDATION.md
@.planning/phases/06-product-surface-hardening-for-cli-doctor-examples-and-docs/06-RESEARCH.md

<interfaces>
<!-- Pattern already used in this repo: tests/pytorch_parity.rs uses cargo test integration layer -->
<!-- CARGO_BIN_EXE_volta is set by cargo when building integration tests -->

Existing integration test pattern (from tests/pytorch_parity.rs structure):
```rust
// tests/cli_smoke.rs
// Run with: cargo test --quiet --test cli_smoke
// Requires: binary built by cargo (CARGO_BIN_EXE_volta)

use std::process::Command;

fn volta_bin() -> &'static str {
    env!("CARGO_BIN_EXE_volta")
}
```

Key env vars relevant to doctor tests:
- MKL_LIB_DIR, MKLROOT, CONDA_PREFIX  (MKL detection)
- LLVM_SYS_210_PREFIX                  (LLVM detection)
- VOLTA_GPU_AVAILABLE                  (GPU env)
- VOLTA_SGD_BACKEND                    (SGD backend switch)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create tests/cli_smoke.rs with all required test stubs</name>
  <files>tests/cli_smoke.rs</files>
  <behavior>
    - smoke_version: `volta version` exits 0 and stdout contains a version string (non-empty)
    - smoke_doctor: `volta doctor` exits 0 and stdout contains "Volta doctor"
    - doctor_json_fields: `volta doctor --json` exits 0 and stdout is parseable as JSON containing keys: "tool", "version", "healthy", "backends"
    - smoke_run_xor: `volta run examples/xor.vt --quiet` exits 0 with no "error" in stderr
    - smoke_check_bench_real: `volta check examples/bench_real.vt --quiet` exits 0 with no "error" in stderr
    - smoke_compile_train_rejects_non_mlp: `volta compile-train examples/transformer_ffn.vt` exits non-zero OR stdout/stderr contains "unsupported" or "MLP-only" (this file is a pure MLP so this test may need to use a file that would be rejected — see note in action)
  </behavior>
  <action>
Create `tests/cli_smoke.rs` as a Rust integration test file. Use the pattern:
```rust
fn volta_bin() -> &'static str { env!("CARGO_BIN_EXE_volta") }
```
to get the built binary path. Set `.current_dir(env!("CARGO_MANIFEST_DIR"))` on every Command so relative paths like `examples/xor.vt` resolve correctly.

For `smoke_compile_train_rejects_non_mlp`: according to Phase 5 / 05-03-PLAN.md and the research, `compile-train` on a non-MLP `.vt` file should fail fast. Check whether `examples/embedding_projector.vt` or a hand-written inline temp file would trigger the rejection. If all example files are actually pure MLP (confirmed in RESEARCH.md), create a minimal temp `.vt` file in the test that includes a `Conv2D` layer or other unsupported op, write it to a temp path, run `compile-train` against it, and assert non-zero exit. Use `std::env::temp_dir()` for the temp file.

For `doctor_json_fields`: parse the stdout as a JSON string manually (no serde_json needed) — assert it contains the literal substrings `"tool"`, `"healthy"`, `"backends"` using `contains()`.

All tests: assert `output.status.success()` or non-success as appropriate. Assert stderr does not contain `"panicked"` on any test. Keep each test function independent (no shared state).

Do NOT use `cargo run` as the test binary launcher — this would trigger a full rebuild on every test run.
  </action>
  <verify>
    <automated>cargo test --quiet --test cli_smoke 2>&1</automated>
  </verify>
  <done>
    `tests/cli_smoke.rs` compiles and all 6 test functions pass. `cargo test --quiet --test cli_smoke` exits 0. No "panicked" in output.
  </done>
</task>

</tasks>

<verification>
`cargo test --quiet --test cli_smoke` passes.
`cargo test --quiet` (full unit suite) still passes — no regressions.
</verification>

<success_criteria>
- `tests/cli_smoke.rs` exists with 6 named test functions matching the verification map
- All tests use `CARGO_BIN_EXE_volta`, not `cargo run`
- `cargo test --quiet --test cli_smoke` exits 0
- `cargo test --quiet` exits 0 (no regressions)
</success_criteria>

<output>
After completion, create `.planning/phases/06-product-surface-hardening-for-cli-doctor-examples-and-docs/06-01-SUMMARY.md`
</output>
