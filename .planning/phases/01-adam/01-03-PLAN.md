---
phase: 01-adam
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
  - 01-02
files_modified:
  - .planning/STATE.md
autonomous: false
requirements:
  - PERF-01
  - PERF-02

must_haves:
  truths:
    - "SGD gate confirmed: B=64 MLP-512 median < 2.10 ms after all Phase 1 changes"
    - "Adam performance confirmed: ≤1.1× ratio vs PyTorch (expected +25% from BENCHMARKS.md)"
    - "STATE.md no longer shows stale 1.9× metric — reflects actual measured performance"
  artifacts:
    - path: ".planning/STATE.md"
      provides: "Accurate performance metrics for future sessions"
      contains: "+25%"
  key_links:
    - from: "bench_official_v2.exe output"
      to: ".planning/STATE.md Performance Metrics table"
      via: "manual copy of measured median values"
      pattern: "Adam vs PyTorch ratio"
---

<objective>
Verify that the SGD regression gate still holds after the AVX-512 fix (Plan 01), and confirm Adam performance is on target. Then update STATE.md to replace the stale 1.9× metric with the actual measured +25% figure. This is the phase sign-off step.

Purpose: Confirm no regression was introduced, close the metrics accuracy gap in STATE.md.
Output: Human-verified benchmark numbers, STATE.md with accurate metrics.
</objective>

<execution_context>
@C:/Users/User/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/User/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/01-adam/01-CONTEXT.md
@.planning/phases/01-adam/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build benchmark binary after AVX-512 fix</name>
  <files>examples/bench_real.train_rust._rust_crate/</files>
  <action>
    Rebuild the benchmark examples to ensure the binary reflects the AVX-512 fix from Plan 01:
    ```
    cd examples/bench_real.train_rust._rust_crate
    cargo build --release --examples
    ```
    This ensures bench_official_v2.exe is built from the updated Cargo.toml (without x86-v4).
    Do NOT run the benchmark yet — just build.
  </action>
  <verify>
    <automated>cd C:/Users/User/Desktop/Volta-main/examples/bench_real.train_rust._rust_crate && cargo build --release --examples 2>&1 | tail -3</automated>
  </verify>
  <done>
    Build exits 0. Binary exists at `examples/bench_real.train_rust._rust_crate/target/release/examples/bench_official_v2.exe`.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    All Phase 1 changes are in place:
    - AVX-512 fix applied to both files (Plan 01)
    - Adam correctness test added and passing (Plan 02)
    - Benchmark binary rebuilt without x86-v4

    Run the benchmark to verify SGD gate and Adam performance:
  </what-built>
  <how-to-verify>
    1. Allow CPU to cool: wait 90 seconds with no heavy workloads running
    2. Run the benchmark:
       ```
       cd examples/bench_real.train_rust._rust_crate
       ./target/release/examples/bench_official_v2.exe
       ```
    3. Check SGD gate: look for "B=64 MLP-512" SGD row — median must be < 2.10 ms
       (primary SGD was 1.703 ms before Phase 1, expect similar or better)
    4. Check Adam: confirm Adam median ≤ PyTorch × 1.1 (expected: Adam ~25% faster)
    5. Record exact median values for STATE.md update

    Expected results (from BENCHMARKS.md 2026-03-06):
    - SGD B=64 MLP-512: ~1.703 ms (PASS, well under 2.10 ms gate)
    - Adam B=64 MLP-512: ~25% faster than PyTorch baseline

    If SGD median is > 2.10 ms: this is a regression — do NOT update STATE.md, report the numbers and stop.
    If results match expectations: reply with the measured medians.
  </how-to-verify>
  <resume-signal>Type "approved: SGD={X}ms Adam={Y}ms" with your measured values, or describe any regression found</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Update STATE.md with accurate metrics</name>
  <files>.planning/STATE.md</files>
  <action>
    Update the Performance Metrics table in STATE.md. Replace the stale Adam ratio row:

    OLD (stale — from before Phase 1 optimization work):
    ```
    | Adam vs PyTorch ratio | 1.9× (поточне) | FAIL — ціль ≤1.1× |
    ```

    NEW (use the actual measured values from the checkpoint above):
    ```
    | Adam vs PyTorch ratio | ~1.25× faster (поточне) | PASS — перевищує ціль ≤1.1× |
    ```
    (Adjust the exact figure based on the checkpoint-verified measurement, e.g., "+25%" or "0.8×")

    Also update the Ключові рішення table row:
    OLD:
    ```
    | Adam без fused GEMM | Поки не реалізовано — ⚠️ 1.9× повільніше |
    ```
    NEW:
    ```
    | Adam MKL cblas_sgemm + AVX2 SIMD + Rayon | Реалізовано — ✓ ~25% швидше PyTorch |
    ```

    Also update the Відомі блокери section — remove the Adam elementwise path bullet (it is no longer a blocker). The AVX-512 bug is also resolved by Plan 01 — do NOT add a new bullet for it (it is fixed, not a blocker).

    Do NOT change any other content in STATE.md.
  </action>
  <verify>
    <automated>grep -n "Adam" C:/Users/User/Desktop/Volta-main/.planning/STATE.md</automated>
  </verify>
  <done>
    STATE.md no longer contains "1.9×". Adam row in Performance Metrics shows PASS status with the measured value. Adam elementwise bullet removed from Відомі блокери.
  </done>
</task>

</tasks>

<verification>
Phase 1 is complete when:
1. `grep -r "x86-v4" src/ examples/bench_real.train_rust._rust_crate/Cargo.toml` → no output
2. `cargo test -p volta -- optimizer::tests` → all pass (including adam_updates_parameter_numerically_correct)
3. Benchmark confirmed by human: SGD B=64 median < 2.10 ms
4. Benchmark confirmed by human: Adam ≤ 1.1× PyTorch (currently ~0.8× i.e. 25% faster)
5. `grep "1.9" .planning/STATE.md` → no output
</verification>

<success_criteria>
- SGD gate verified: B=64 MLP-512 median confirmed < 2.10 ms after all changes
- Adam gate verified: ≤1.1× vs PyTorch (currently exceeds — ~0.8×)
- STATE.md reflects actual measured numbers, not stale pre-optimization figures
- PERF-01 and PERF-02 requirements are observably satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/01-adam/01-03-SUMMARY.md` using the summary template.
</output>
