---
phase: 07-packaging-and-install-story-for-distributable-releases
plan: 03
type: execute
wave: 1
depends_on:
  - 07-01
files_modified:
  - README.md
autonomous: true
requirements:
  - DIST-V2-02

must_haves:
  truths:
    - "README.md has an 'Install' section placed before the Quickstart section"
    - "Install section links to https://github.com/BetterCrusader/Volta/releases/latest"
    - "Install section shows all 3 artifact filenames matching Plan 07-01 naming: linux-x86_64.tar.gz, macos-universal.tar.gz, windows-x86_64.zip"
    - "Install section gives unpack + PATH steps for Linux/macOS (tar xzf + chmod + mv)"
    - "Install section gives unpack + PATH step for Windows (unzip)"
    - "Install section has a Verify block with volta --help, volta doctor, volta run examples/xor.vt"
    - "Install section states no Rust toolchain required for prebuilt binaries"
    - "Platform note section updated: Linux and macOS now say CI-tested instead of untested"
  artifacts:
    - path: "README.md"
      provides: "Install section for prebuilt binaries"
      contains: "releases/latest"
  key_links:
    - from: "README Install artifact table"
      to: "release.yml artifact names (Plan 07-01)"
      via: "exact filenames: linux-x86_64, macos-universal, windows-x86_64"
      pattern: "macos-universal"
---

<objective>
Add an "Install" section to README.md documenting the prebuilt binary install story, and update the existing platform note to reflect that Linux/macOS are now CI-tested.

Purpose: DIST-V2-02 requires the install story documented with steps that actually work. Currently README.md says "build from source only" and "not tested on macOS or Linux". Phase 7 changes both facts.

Output: README.md with a new "Install" section (positioned before Quickstart) and an updated "Platform note" section.
</objective>

<execution_context>
@C:/Users/User/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/User/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-packaging-and-install-story-for-distributable-releases/07-CONTEXT.md
@.planning/phases/07-packaging-and-install-story-for-distributable-releases/07-01-SUMMARY.md
@README.md

<interfaces>
<!-- Artifact names from Plan 07-01: -->
<!--   Linux:   volta-{tag}-linux-x86_64.tar.gz   -->
<!--   macOS:   volta-{tag}-macos-universal.tar.gz  -->
<!--   Windows: volta-{tag}-windows-x86_64.zip      -->

<!-- GitHub repo URL: https://github.com/BetterCrusader/Volta -->
<!-- Releases URL:    https://github.com/BetterCrusader/Volta/releases/latest -->

<!-- Current README section order: -->
<!--   # Volta                          -->
<!--   ## Why Volta exists              -->
<!--   ## Current strengths / limitations -->
<!--   ---                              -->
<!--   ## Quickstart                    -->
<!--   ---                              -->
<!--   ## Benchmark highlights          -->
<!--   ## Architecture at a glance      -->
<!--   ## CLI                           -->
<!--   ## Language example              -->
<!--   ---                              -->
<!--   ## Install / platform note       (this is the section to rename/update) -->
<!--   ## Roadmap snapshot              -->
<!--   ## License                       -->

<!-- User decision: Install section is "first thing users see" — place before Quickstart -->
<!-- User decision: cargo install is NOT the primary path, do not list it -->
<!-- Note: volta run examples/xor.vt requires the repo's examples/ dir (state this) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Install section and update platform note in README.md</name>
  <files>README.md</files>
  <action>
    Make two targeted edits to README.md.

    EDIT 1: Insert a new "## Install" section between the "---" separator after "## Current strengths / current limitations" and the "## Quickstart" heading.

    The new Install section text (insert before "## Quickstart"):

      ## Install

      Download the prebuilt binary for your platform from the
      [latest release](https://github.com/BetterCrusader/Volta/releases/latest):

      | Platform | File |
      |----------|------|
      | Linux x86-64 | `volta-vX.Y.Z-linux-x86_64.tar.gz` |
      | macOS (Apple Silicon + Intel) | `volta-vX.Y.Z-macos-universal.tar.gz` |
      | Windows x86-64 | `volta-vX.Y.Z-windows-x86_64.zip` |

      Replace `vX.Y.Z` with the release version shown on the releases page.

      **Linux / macOS:**

          tar xzf volta-vX.Y.Z-*.tar.gz
          chmod +x volta
          mv volta /usr/local/bin/    # or any directory already on your PATH
          volta --help

      **Windows:**
      Unzip `volta-vX.Y.Z-windows-x86_64.zip`, then move `volta.exe` to a
      directory on your `PATH` (or add its folder to `PATH`).

      **Verify the install:**

          volta --help
          volta doctor
          volta run examples/xor.vt   # requires the examples/ directory from the repo

      > `volta run` requires a `.vt` source file on disk. The binary is
      > self-contained; example files are in the repository.

      No Rust toolchain required to run prebuilt binaries.

      ---

    EDIT 2: Replace the existing "## Install / platform note" section entirely. Find the heading "## Install / platform note" and replace the entire section (heading + bullet list) with:

      ## Platform note

      - **Windows x86-64**: fully tested
      - **Linux x86-64**: builds and interpreter path tested in CI (Ubuntu runner)
      - **macOS (Apple Silicon + Intel)**: universal binary tested in CI (macOS runner)
      - **LLVM**: required only for `volta compile` (inference DLL). Set `LLVM_SYS_210_PREFIX`
        or place `clang` next to the binary.
      - **MKL**: required for `compile-train --rust`. `volta doctor` shows status and install
        instructions if missing.
      - **CUDA**: build with `--features cuda`. GPU perf not benchmarked.

    Do NOT change any other section: Quickstart, benchmark tables, architecture diagram,
    CLI section, language example, roadmap snapshot, or license.
  </action>
  <verify>
    <automated>python3 -c "
content = open('README.md').read()
assert 'releases/latest' in content, 'missing releases link'
assert 'macos-universal' in content, 'missing macos-universal artifact name'
assert 'linux-x86_64' in content, 'missing linux artifact name'
assert 'windows-x86_64' in content, 'missing windows artifact name'
assert 'volta --help' in content, 'missing help verify step'
assert 'volta doctor' in content, 'missing doctor verify step'
assert 'examples/xor.vt' in content, 'missing xor.vt verify step'
assert '## Install' in content, 'missing Install section heading'
assert 'No Rust toolchain' in content, 'missing no-toolchain note'
assert 'tested in CI' in content, 'platform note not updated'
idx_install = content.index('## Install')
idx_quickstart = content.index('## Quickstart')
assert idx_install < idx_quickstart, 'Install section must come before Quickstart'
print('README OK')
"</automated>
  </verify>
  <done>
    README.md has:
    - "## Install" section appearing before "## Quickstart"
    - Artifact table with linux-x86_64, macos-universal, windows-x86_64 filenames
    - Unpack steps for Linux/macOS and Windows
    - Verify block with volta --help, volta doctor, volta run examples/xor.vt
    - "No Rust toolchain required" statement
    - "## Platform note" section stating Linux and macOS are CI-tested
  </done>
</task>

</tasks>

<verification>
After task completion:
1. python3 verify script passes (see automated step)
2. "## Install" appears before "## Quickstart" in the file
3. The old "## Install / platform note" heading is gone, replaced by "## Platform note"
4. "Not tested on macOS or Linux" is no longer in the file
5. "No installer, no package" is no longer in the Limitations list (update that bullet too: replace "No installer, no package, no Python bindings — build from source only" with "Prebuilt binaries available for Linux, macOS, and Windows — see Install section. No Python bindings.")
</verification>

<success_criteria>
- README.md "## Install" section exists before "## Quickstart"
- Three artifact filenames match Plan 07-01 naming convention
- Linux/macOS/Windows install steps are correct and minimal
- Verify block lists all 3 smoke commands
- Platform note updated to "tested in CI" for Linux and macOS
- "build from source only" limitation bullet updated to point to Install section
</success_criteria>

<output>
After completion, create `.planning/phases/07-packaging-and-install-story-for-distributable-releases/07-03-SUMMARY.md`
</output>
